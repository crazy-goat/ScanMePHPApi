services:
  webman:
    build: .
    ports:
      - "8787:8787"
    environment:
      - APP_ENV=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "php", "healthcheck.php"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
